@model IEnumerable<EventsPlus.Models.EventCompany>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContactInformation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address.AddressID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactInformation.ContactInformationID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EventCompanyID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EventCompanyID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EventCompanyID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
